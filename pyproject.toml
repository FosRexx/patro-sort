[project]
name = "bs-media-sorter"
version = "0.1.0"
description = "Sort images/videos into Bikram Samvat (BS) folder structures."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "nepali-datetime>=1.0.8.4",
    "pyexiftool>=0.5.6",
    "pystatx>=0.1",
    "python-dateutil>=2.9.0.post0",
]
